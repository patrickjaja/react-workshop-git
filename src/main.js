import React from 'react';
import ReactDOM from 'react-dom';

import LoginController from './LoginController';

const mountNode = document.getElementById('mount');
ReactDOM.render(<LoginController/>, mountNode);
